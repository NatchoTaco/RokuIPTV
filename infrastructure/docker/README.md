# Docker Infrastructure

Docker Compose is the canonical runtime for Milestones 1 and 2. The API Dockerfile is reused by the background worker, and uploaded playlist files are stored in the `streamforge_source_uploads` volume.

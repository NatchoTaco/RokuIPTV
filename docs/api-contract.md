# StreamForge API Contract

All API endpoints use the `/api/v1` prefix. The OpenAPI schema is generated by the FastAPI application and stored in `packages/api-contract/openapi.json` when exported.

## Implemented Endpoints

### Health

- `GET /api/v1/health`
- `GET /api/v1/readiness`

Health reports service status, version, database reachability, Redis reachability, and setup completion state.

### Authentication

- `POST /api/v1/auth/bootstrap-admin`
- `POST /api/v1/auth/sign-in`
- `POST /api/v1/auth/sign-out`
- `GET /api/v1/auth/me`

Bootstrap administrator creation is allowed only before setup is complete and only when no administrator exists.

### Setup

- `GET /api/v1/setup/state`
- `PATCH /api/v1/setup/state`

Setup state persists first-run wizard progress and installation mode. Milestone 1 supports account and installation-mode steps.

### Settings and Diagnostics

- `GET /api/v1/settings/public`
- `GET /api/v1/diagnostics/summary`

Milestone 1 exposes only non-sensitive settings and diagnostic status.

### Sources

- `GET /api/v1/sources`
- `POST /api/v1/sources/validate-url`
- `POST /api/v1/sources/validate-upload`
- `POST /api/v1/sources/m3u-url`
- `POST /api/v1/sources/m3u-upload`
- `POST /api/v1/sources/demo`
- `PATCH /api/v1/sources/{source_id}`
- `DELETE /api/v1/sources/{source_id}`
- `POST /api/v1/sources/{source_id}/refresh`
- `GET /api/v1/sources/{source_id}/imports`

Source endpoints require an authenticated administrator. Raw source secrets are never returned.

### Playlists

- `GET /api/v1/playlists/imports`
- `GET /api/v1/playlists/jobs/{job_id}`
- `GET /api/v1/playlists/synthetic-demo.m3u`

Playlist endpoints require an authenticated administrator.

## Milestone 2 Source Payloads

`POST /api/v1/sources/validate-url` accepts:

- `url`: HTTP or HTTPS playlist URL.

`POST /api/v1/sources/validate-upload` accepts multipart form data:

- `file`: uploaded M3U file.

Validation responses include:

- `playlist_reachable`
- `channel_count`
- `group_count`
- `estimated_import_time_seconds`
- `warnings`
- `errors`
- `checksum`
- `source_version`

`POST /api/v1/sources/m3u-url` accepts:

- `name`
- `url`
- `refresh_interval_minutes`

`POST /api/v1/sources/m3u-upload` accepts multipart form data:

- `name`
- `refresh_interval_minutes`
- `file`

Creation responses include the created source summary plus the queued import job.

Import history responses include:

- start and completion times
- duration
- channels and groups imported
- warnings and failures
- checksum
- source version

## Future Endpoint Areas

The specification reserves these `/api/v1` areas for later milestones:

- `/epg-sources`
- `/channels`
- `/channel-groups`
- `/cleanup`
- `/guide`
- `/recordings`
- `/recording-rules`
- `/devices`
- `/pairing`
- `/profiles`
- `/favorites`
- `/playback`
- `/storage`

Deferred endpoints must not return fake successful behavior. They should be absent or clearly marked as not implemented until their milestone begins.

## Dashboard Contract Strategy

The dashboard uses typed fetch helpers and Zod schemas for runtime validation. Generated types from OpenAPI can replace or supplement these helpers after the schema export workflow is stable.
